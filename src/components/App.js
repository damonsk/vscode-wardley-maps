import React from 'react';
import { hot } from 'react-hot-loader/root';

const App = props => {

    return (
		<React.Fragment>
            <span>hey</span>
        </React.Fragment>
    )
}

export default hot(App);